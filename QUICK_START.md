# 🚀 Быстрый старт для Артишок

## 1️⃣ УСТАНОВКА

```bash
# Установи зависимости
pip install -r requirements.txt

# Скопируй настройки
cp .env.example .env
```

## 2️⃣ НАСТРОЙКА .env

Открой `.env` и заполни:

```env
# Токен бота (получи у @BotFather)
BOT_TOKEN=your_bot_token

# Твой Telegram ID (узнай у @userinfobot)
ADMIN_IDS=123456789

# ID канала (см. инструкцию ниже)
MAIN_CHANNEL_ID=-1001234567890

# Prodamus (у вас уже есть доступ)
PRODAMUS_SECRET_KEY=ключ_из_продамуса
```

---

## 3️⃣ ПОЛУЧЕНИЕ ID КАНАЛА

### Вариант 1 (простой):
1. Перешли любое сообщение из канала боту [@userinfobot](https://t.me/userinfobot)
2. Он покажет ID канала (начинается с `-100`)
3. Вставь в `.env`: `MAIN_CHANNEL_ID=-1001234567890`

### Вариант 2 (через код):
Запусти временный скрипт:

```python
# get_channel_id.py
import asyncio
from aiogram import Bot

async def main():
    bot = Bot(token="ВАШ_ТОКЕН_БОТА")
    
    # Вставь ссылку на канал
    invite_link = "https://t.me/+JBTBkhpXZjplNGQ6"
    
    # Бот должен быть уже добавлен в канал как админ!
    # Потом можно получить апдейты и увидеть chat_id
    
    me = await bot.get_me()
    print(f"Бот: @{me.username}")
    print(f"Теперь перешли сообщение из канала боту и запусти его")

asyncio.run(main())
```

---

## 4️⃣ ДОБАВЛЕНИЕ БОТА В КАНАЛ

**Важно!** Бот должен быть админом канала:

1. Зайди в канал: https://t.me/+JBTBkhpXZjplNGQ6
2. Настройки канала → Администраторы → Добавить администратора
3. Найди своего бота
4. Дай права:
   - ✅ **Добавление участников**
   - ✅ **Удаление участников**
   - ✅ Создание инвайт-ссылок

---

## 5️⃣ МИГРАЦИЯ БАЗЫ

```bash
python scripts/migrate_from_bothelp.py 2Fwh-export-31012026.csv
```

Скрипт:
- Импортирует всех 43 пользователей
- Определит активные подписки по тегам
- Покажет статистику

---

## 6️⃣ ЗАПУСК

```bash
# Терминал 1 - Бот
python main.py

# Терминал 2 - Webhook (для приема платежей)
python main.py webhook
```

---

## 7️⃣ ПРОВЕРКА

1. Напиши боту `/start`
2. Напиши `/admin` - должна открыться админка
3. Проверь статистику - должно быть 43 пользователя

---

## 📝 ВАЖНЫЕ КОМАНДЫ

### Для админа:
- `/admin` - админ-панель
- `📊 Статистика` - общая статистика
- `👥 Пользователи` - список юзеров
- `🎁 Промокоды` - создать/посмотреть промокоды
- `📢 Рассылка` - массовая рассылка
- `📥 Экспорт базы` - скачать Excel

### Для пользователей:
- `/start` - главное меню
- `💳 Купить подписку` - выбор тарифа
- `📊 Моя подписка` - проверить статус
- `🎁 Промокод` - ввести промокод

---

## 🔐 ДОСТУПЫ PRODAMUS

```
Логин: info@artishokcenter.ru
Пароль: &Ll1icqPF)
```

**Что настроить в Prodamus:**
1. Webhook URL: `https://ваш-домен.ru/webhook/prodamus`
2. Передача параметра `user_id` при оплате
3. Секретный ключ скопировать в `.env`

---

## ❓ Если что-то не работает

1. **Бот не отвечает:**
   - Проверь токен в `.env`
   - Убедись что бот запущен: `python main.py`

2. **Не добавляет в канал:**
   - Проверь ID канала (должен начинаться с `-100`)
   - Убедись что бот админ канала с нужными правами

3. **Не работают платежи:**
   - Проверь webhook в Prodamus
   - Запусти `python main.py webhook`
   - Webhook должен быть доступен извне (нужен публичный домен)

---

## 📞 Поддержка

Telegram: [@n0face_bot](https://t.me/n0face_bot)
