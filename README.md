# 🎨 Shmukler Art Club Bot

Telegram-бот для автоматизации работы закрытого арт-клуба с интеграцией платежной системы Prodamus.

## 📋 Возможности

### Для пользователей:
- ✅ Покупка подписки (1, 3, 6, 12 месяцев)
- 🎁 Активация промокодов
- 📊 Просмотр статуса подписки
- 💳 Автоматический вход в канал после оплаты
- ⏰ Уведомления об истечении подписки

### Для администраторов:
- 👥 Управление пользователями
- 🎁 Создание промокодов (скидки, бесплатные подписки)
- 📢 Массовые рассылки с таргетингом
- 📊 Статистика и аналитика
- 📥 Экспорт базы в Excel
- 💰 Финансовые отчеты

### Автоматизация:
- 🤖 Автоматическая выдача доступа при оплате
- 🚫 Автоматическое удаление из канала при истечении подписки
- 📬 Напоминания за 3 дня до истечения подписки

---

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Создайте виртуальное окружение
python -m venv venv

# Активируйте его
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 2. Настройка окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Заполните обязательные параметры:

```env
# Получите токен у @BotFather
BOT_TOKEN=your_telegram_bot_token_here

# ID админов (через запятую, получить: напишите боту, он покажет ID)
ADMIN_IDS=123456789,987654321

# ID канала клуба (добавьте бота в канал как админа и получите ID)
MAIN_CHANNEL_ID=-1001234567890

# Секретный ключ Prodamus (из личного кабинета)
PRODAMUS_SECRET_KEY=your_prodamus_secret_key
```

### 3. Запуск бота

```bash
# Запуск только бота (для тестирования)
python main.py

# Запуск webhook-сервера (для приема платежей)
python main.py webhook
```

**Для production рекомендуется запускать в отдельных терминалах:**

```bash
# Терминал 1 - Бот
python main.py

# Терминал 2 - Webhook сервер
python main.py webhook
```

---

## 🔧 Настройка Telegram

### Создание бота

1. Напишите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Укажите название и username бота
4. Скопируйте токен в `.env` файл

### Настройка канала

1. Создайте приватный канал для клуба
2. Добавьте бота как администратора с правами:
   - ✅ Добавление участников
   - ✅ Удаление участников
3. Получите ID канала:
   - Перешлите любое сообщение из канала боту [@userinfobot](https://t.me/userinfobot)
   - Скопируйте ID (начинается с `-100`)
   - Укажите в `.env`: `MAIN_CHANNEL_ID=-1001234567890`

### Получение ID администратора

1. Напишите боту `/start`
2. Бот покажет ваш Telegram ID
3. Добавьте ID в `.env`: `ADMIN_IDS=123456789`

---

## 💳 Интеграция с Prodamus

### Настройка webhook

1. Войдите в личный кабинет [Prodamus](https://prodamus.ru)
2. Перейдите в настройки магазина
3. Укажите URL webhook: `https://yourdomain.com/webhook/prodamus`
4. Скопируйте секретный ключ в `.env`

### Создание платежных ссылок

Бот автоматически генерирует ссылки с параметрами:

```
https://artclub.pay.prodamus.ru/buy/1_month?user_id=123456789
```

**Важно:** В Prodamus настройте передачу параметра `user_id` для идентификации плательщика.

### Тестирование платежей

1. Используйте тестовый режим Prodamus
2. Тестовая карта: `5555 5555 5555 4444`
3. Проверьте получение webhook в логах

---

## 🗂️ Структура проекта

```
artclub_bot/
├── bot/
│   ├── database/
│   │   ├── models.py          # Модели БД (User, Subscription, Promocode, etc)
│   │   └── database.py        # Методы работы с БД
│   ├── handlers/
│   │   ├── user.py            # Хэндлеры для пользователей
│   │   └── admin.py           # Админ-панель
│   ├── keyboards/
│   │   ├── user.py            # Клавиатуры для пользователей
│   │   └── admin.py           # Клавиатуры админки
│   └── utils/
│       ├── scheduler.py       # Планировщик проверки подписок
│       └── helpers.py         # Вспомогательные функции
├── webhook/
│   └── prodamus.py            # FastAPI сервер для webhook
├── scripts/
│   └── migrate_from_bothelp.py  # Миграция с BotHelp
├── main.py                    # Точка входа
├── requirements.txt
├── .env.example
└── README.md
```

---

## 👨‍💼 Админ-панель

### Вход в админку

```
/admin
```

### Создание промокода

1. Админ-панель → 🎁 Промокоды → Создать
2. Выберите тип:
   - **Бесплатная подписка** - 100% скидка
   - **Процентная скидка** - скидка в %
   - **Фиксированная скидка** - скидка в рублях
3. Укажите параметры (длительность, лимит использований)
4. Промокод сгенерируется автоматически

**Пример промокода:**
- Код: `ARTCLUB2026`
- Тип: Бесплатная подписка
- Длительность: 1 месяц
- Лимит: 100 использований

### Рассылка сообщений

1. Админ-панель → 📢 Рассылка
2. Выберите аудиторию:
   - Все пользователи
   - Только активные подписчики
   - Только с истекшей подпиской
3. Отправьте сообщение (текст, фото, видео)
4. Подтвердите отправку

### Экспорт базы

Админ-панель → 📥 Экспорт базы

Получите Excel-файл со всеми пользователями:
- ID, Username, Имя, Фамилия
- Статус подписки, Дата окончания
- Дата регистрации

---

## 🔄 Миграция с BotHelp

### Подготовка данных

1. Экспортируйте пользователей из BotHelp в CSV
2. Формат CSV:
   ```
   user_id,username,first_name,last_name,subscription_until
   123456789,johndoe,John,Doe,2026-03-15
   ```

### Запуск миграции

```bash
python scripts/migrate_from_bothelp.py users_export.csv
```

### После миграции

1. Проверьте импортированных пользователей в админке
2. Создайте рассылку с инструкциями:
   - Как войти в нового бота
   - Ссылка на бота
   - Инвайт-ссылка в канал (для активных подписчиков)

---

## 🐳 Деплой на сервер

### Вариант 1: VPS с systemd

1. Установите зависимости на сервере:
   ```bash
   sudo apt update
   sudo apt install python3-pip python3-venv
   ```

2. Клонируйте проект:
   ```bash
   git clone <your-repo-url>
   cd artclub_bot
   python3 -m venv venv
   source venv/bin/activate
   pip install -r requirements.txt
   ```

3. Создайте systemd сервисы:

**Файл: `/etc/systemd/system/artclub-bot.service`**
```ini
[Unit]
Description=Shmukler Art Club Bot
After=network.target

[Service]
Type=simple
User=youruser
WorkingDirectory=/home/youruser/artclub_bot
Environment="PATH=/home/youruser/artclub_bot/venv/bin"
ExecStart=/home/youruser/artclub_bot/venv/bin/python main.py
Restart=always

[Install]
WantedBy=multi-user.target
```

**Файл: `/etc/systemd/system/artclub-webhook.service`**
```ini
[Unit]
Description=Shmukler Art Club Webhook
After=network.target

[Service]
Type=simple
User=youruser
WorkingDirectory=/home/youruser/artclub_bot
Environment="PATH=/home/youruser/artclub_bot/venv/bin"
ExecStart=/home/youruser/artclub_bot/venv/bin/python main.py webhook
Restart=always

[Install]
WantedBy=multi-user.target
```

4. Запустите сервисы:
   ```bash
   sudo systemctl daemon-reload
   sudo systemctl enable artclub-bot artclub-webhook
   sudo systemctl start artclub-bot artclub-webhook
   ```

5. Настройте Nginx для webhook (опционально):

**/etc/nginx/sites-available/artclub**
```nginx
server {
    listen 80;
    server_name yourdomain.com;

    location /webhook/ {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

### Вариант 2: Docker

**Dockerfile (скоро будет добавлен)**

---

## 📊 Мониторинг

### Просмотр логов

```bash
# Логи бота
tail -f bot.log

# Логи systemd (если используете)
sudo journalctl -u artclub-bot -f
sudo journalctl -u artclub-webhook -f
```

### Проверка работоспособности

```bash
# Webhook health check
curl http://localhost:8000/health

# Проверка БД
sqlite3 artclub.db "SELECT COUNT(*) FROM users;"
```

---

## 🛠️ Разработка

### Добавление новых функций

1. Создайте новый хэндлер в `bot/handlers/`
2. Зарегистрируйте роутер в `main.py`
3. Добавьте клавиатуру в `bot/keyboards/`

### Тестирование

```bash
# Запуск в режиме отладки
DEBUG=True python main.py
```

### База данных

- **SQLite** (по умолчанию) - для разработки и малых проектов
- **PostgreSQL** (рекомендуется для production):
  ```env
  DATABASE_URL=postgresql+asyncpg://user:password@localhost/artclub
  ```

---

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи: `tail -f bot.log`
2. Убедитесь, что все переменные в `.env` заполнены
3. Проверьте права бота в канале
4. Убедитесь, что webhook доступен извне

**Контакты:**
- Telegram: [@n0face_bot](https://t.me/n0face_bot)
- Email: support@noface.digital
- Сайт: [noface.digital](https://noface.digital)

---

## 📝 Лицензия

Проект разработан для **Культурного центра "Артишок"** (Shmukler Art Club).

© 2026 NoFace Digital. Все права защищены.

---

## 🎯 Roadmap

- [ ] Рекуррентные платежи (автопродление)
- [ ] Интеграция с другими платежными системами (ЮKassa, Stripe)
- [ ] Многоуровневые подписки (Basic, Premium, VIP)
- [ ] Реферальная система
- [ ] Gamification (бейджи, достижения)
- [ ] Интеграция с календарем Google (расписание мероприятий)
- [ ] Мобильное приложение

---

**Сделано с ❤️ для ценителей искусства**
